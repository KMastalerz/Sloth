import { DynamicPanelDirective } from './dynamic-panel.directive';

describe('DynamicPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicPanelDirective();
    expect(directive).toBeTruthy();
  });
});
