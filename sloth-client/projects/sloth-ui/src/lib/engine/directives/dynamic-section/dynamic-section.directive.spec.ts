import { DynamicSectionDirective } from './dynamic-section.directive';

describe('DynamicSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicSectionDirective();
    expect(directive).toBeTruthy();
  });
});
